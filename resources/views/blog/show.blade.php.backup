<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="{{ asset('logo flower.png') }}">
    
    <!-- Primary Meta Tags -->
    <title>{{ $post->title }} | CAS Private Care LLC Blog</title>
    <meta name="title" content="{{ $post->title }} | CAS Private Care LLC Blog">
    <meta name="description" content="{{ $post->excerpt }}">
    @php
        $postTags = is_array($post->tags) ? $post->tags : (is_string($post->tags) ? json_decode($post->tags, true) : []);
    @endphp
    <meta name="keywords" content="{{ $postTags ? implode(', ', $postTags) : 'caregiving, home care, elderly care' }}">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="{{ url('/blog/' . $post->slug) }}">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{ url('/blog/' . $post->slug) }}">
    <meta property="og:title" content="{{ $post->title }}">
    <meta property="og:description" content="{{ $post->excerpt }}">
    <meta property="og:image" content="{{ $post->featured_image }}">
    <meta property="article:published_time" content="{{ $post->published_at->toIso8601String() }}">
    <meta property="article:author" content="{{ $post->author_name }}">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ url('/blog/' . $post->slug) }}">
    <meta property="twitter:title" content="{{ $post->title }}">
    <meta property="twitter:description" content="{{ $post->excerpt }}">
    <meta property="twitter:image" content="{{ $post->featured_image }}">
    
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700;800&family=Playfair+Display:wght@400;500;600;700;800&family=Merriweather:wght@300;400;700&display=swap" rel="stylesheet">
    
    @include('partials.nav-footer-styles')
    
    <style>
        body {
            font-family: 'Sora', sans-serif;
            margin: 0;
            padding: 0;
            line-height: 1.8;
            color: #1e293b;
            background-color: #ffffff;
        }

        .article-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem;
        }

        .article-header {
            text-align: center;
            margin-bottom: 3rem;
            padding-top: 2rem;
        }

        .article-category {
            display: inline-block;
            background: #3b82f6;
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            font-size: 0.875rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 1.5rem;
        }

        .article-title {
            font-family: 'Playfair Display', serif;
            font-size: 3.5rem;
            font-weight: 800;
            color: #1e293b;
            line-height: 1.2;
            margin-bottom: 1.5rem;
        }

        .article-excerpt {
            font-size: 1.35rem;
            color: #64748b;
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        .article-meta {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            flex-wrap: wrap;
            padding: 1.5rem;
            background: #f8fafc;
            border-radius: 12px;
            margin-bottom: 2rem;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: #64748b;
            font-size: 0.95rem;
        }

        .meta-item i {
            color: #3b82f6;
            font-size: 1.1rem;
        }

        .author-section {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            margin: 2rem 0;
            padding: 1.5rem;
            background: linear-gradient(135deg, #dbeafe 0%, #f0f9ff 100%);
            border-radius: 16px;
        }

        .author-avatar {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid white;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .author-details h3 {
            margin: 0;
            font-size: 1.1rem;
            color: #1e293b;
            font-weight: 700;
        }

        .author-details p {
            margin: 0.25rem 0 0;
            color: #64748b;
            font-size: 0.9rem;
        }

        .featured-image-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto 3rem;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
        }

        .featured-image {
            width: 100%;
            height: auto;
            display: block;
        }

        .article-content {
            font-family: 'Merriweather', serif;
            font-size: 1.15rem;
            line-height: 1.9;
            color: #334155;
            margin: 3rem 0;
        }

        .article-content h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.25rem;
            font-weight: 700;
            color: #1e293b;
            margin: 3rem 0 1.5rem;
            line-height: 1.3;
        }

        .article-content h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.75rem;
            font-weight: 600;
            color: #334155;
            margin: 2.5rem 0 1rem;
        }

        .article-content p {
            margin-bottom: 1.8rem;
        }

        .article-content img {
            width: 100%;
            height: auto;
            border-radius: 16px;
            margin: 2.5rem 0;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .article-content ul,
        .article-content ol {
            margin: 2rem 0;
            padding-left: 2rem;
        }

        .article-content li {
            margin-bottom: 1rem;
            line-height: 1.8;
        }

        .article-content blockquote {
            border-left: 5px solid #3b82f6;
            background: #f8fafc;
            padding: 1.5rem 2rem;
            margin: 2.5rem 0;
            border-radius: 0 12px 12px 0;
            font-style: italic;
            font-size: 1.25rem;
            color: #475569;
        }

        .article-content a {
            color: #3b82f6;
            text-decoration: underline;
            font-weight: 600;
        }

        .article-content a:hover {
            color: #2563eb;
        }

        .article-content strong {
            color: #1e293b;
            font-weight: 700;
        }

        .content-highlight {
            background: #fef3c7;
            padding: 2rem;
            border-radius: 16px;
            margin: 2.5rem 0;
            border-left: 5px solid #fbbf24;
        }

        .content-highlight h4 {
            font-family: 'Playfair Display', serif;
            color: #92400e;
            margin-top: 0;
            font-size: 1.5rem;
        }

        .tags-section {
            margin: 3rem 0;
            padding: 2rem;
            background: #f8fafc;
            border-radius: 16px;
        }

        .tags-section h4 {
            font-size: 1rem;
            color: #64748b;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .tag-list {
            display: flex;
            flex-wrap: wrap;
            gap: 0.75rem;
        }

        .tag {
            background: white;
            color: #3b82f6;
            padding: 0.6rem 1.2rem;
            border-radius: 50px;
            font-size: 0.9rem;
            font-weight: 600;
            border: 2px solid #dbeafe;
            transition: all 0.3s ease;
        }

        .tag:hover {
            background: #3b82f6;
            color: white;
            transform: translateY(-2px);
        }

        .share-section {
            text-align: center;
            padding: 3rem 2rem;
            background: linear-gradient(135deg, #3b82f6 0%, #1e40af 100%);
            color: white;
            border-radius: 20px;
            margin: 3rem 0;
        }

        .share-section h3 {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            margin-bottom: 1.5rem;
        }

        .share-buttons {
            display: flex;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .share-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 1rem 2rem;
            background: white;
            color: #3b82f6;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .share-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }

        .share-btn.facebook { background: #1877f2; color: white; }
        .share-btn.twitter { background: #1da1f2; color: white; }
        .share-btn.linkedin { background: #0077b5; color: white; }
        .share-btn.whatsapp { background: #25d366; color: white; }

        .related-posts {
            margin: 5rem 0;
        }

        .related-posts h3 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            font-weight: 700;
            color: #1e40af;
            text-align: center;
            margin-bottom: 3rem;
        }

        .related-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .related-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            text-decoration: none;
            display: block;
        }

        .related-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 50px rgba(0,0,0,0.15);
        }

        .related-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .related-card-content {
            padding: 1.5rem;
        }

        .related-card h4 {
            font-family: 'Playfair Display', serif;
            font-size: 1.25rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.75rem;
            line-height: 1.3;
        }

        .related-card p {
            color: #64748b;
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .back-to-blog {
            text-align: center;
            margin: 4rem 0;
        }

        .back-to-blog a {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 1rem 2.5rem;
            background: #f1f5f9;
            color: #334155;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .back-to-blog a:hover {
            background: #3b82f6;
            color: white;
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .article-title {
                font-size: 2.5rem;
            }

            .article-excerpt {
                font-size: 1.15rem;
            }

            .article-content {
                font-size: 1.05rem;
            }

            .article-meta {
                gap: 1rem;
            }

            .share-buttons {
                flex-direction: column;
            }

            .share-btn {
                width: 100%;
                justify-content: center;
            }

            .related-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>

    <!-- Schema.org Article Markup -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Article",
        "headline": "{{ $post->title }}",
        "description": "{{ $post->excerpt }}",
        "image": "{{ $post->featured_image }}",
        "author": {
            "@type": "Person",
            "name": "{{ $post->author_name }}"
        },
        "publisher": {
            "@type": "Organization",
            "name": "CAS Private Care LLC",
            "logo": {
                "@type": "ImageObject",
                "url": "{{ asset('logo flower.png') }}"
            }
        },
        "datePublished": "{{ $post->published_at->toIso8601String() }}",
        "dateModified": "{{ $post->updated_at->toIso8601String() }}"
    }
    </script>
</head>
<body>
    @include('partials.navigation')

    <main style="padding-top: 88px;">
        <article class="article-container">
            <!-- Article Header -->
            <header class="article-header">
                <span class="article-category">{{ $post->category }}</span>
                <h1 class="article-title">{{ $post->title }}</h1>
                <p class="article-excerpt">{{ $post->excerpt }}</p>
                
                <div class="article-meta">
                    <span class="meta-item">
                        <i class="bi bi-calendar3"></i>
                        {{ $post->formatted_date }}
                    </span>
                    <span class="meta-item">
                        <i class="bi bi-clock"></i>
                        {{ $post->read_time_text }}
                    </span>
                    <span class="meta-item">
                        <i class="bi bi-geo-alt"></i>
                        {{ $post->location }}
                    </span>
                    <span class="meta-item">
                        <i class="bi bi-eye"></i>
                        {{ number_format($post->views) }} views
                    </span>
                </div>

                <div class="author-section">
                    <img src="{{ $post->author_image ?? asset('logo flower.png') }}" alt="{{ $post->author_name }}" class="author-avatar">
                    <div class="author-details">
                        <h3>{{ $post->author_name }}</h3>
                        <p>Professional Caregiving Expert</p>
                    </div>
                </div>
            </header>

            <!-- Featured Image -->
            <div class="featured-image-container">
                <img src="{{ $post->featured_image }}" alt="{{ $post->title }}" class="featured-image">
            </div>

            <!-- Article Content -->
            <div class="article-content">
                {!! $post->content !!}
            </div>

            <!-- Tags -->
            @php
                $postTags = is_array($post->tags) ? $post->tags : (is_string($post->tags) ? json_decode($post->tags, true) : []);
            @endphp
            @if($postTags && count($postTags) > 0)
            <div class="tags-section">
                <h4>Related Topics</h4>
                <div class="tag-list">
                    @foreach($postTags as $tag)
                        <span class="tag">{{ $tag }}</span>
                    @endforeach
                </div>
            </div>
            @endif

            <!-- Share Section -->
            <div class="share-section">
                <h3>Share This Article</h3>
                <p style="margin-bottom: 2rem; opacity: 0.9;">Help others discover valuable caregiving insights</p>
                <div class="share-buttons">
                    <a href="https://www.facebook.com/sharer/sharer.php?u={{ urlencode(url('/blog/' . $post->slug)) }}" target="_blank" class="share-btn facebook">
                        <i class="bi bi-facebook"></i> Facebook
                    </a>
                    <a href="https://twitter.com/intent/tweet?url={{ urlencode(url('/blog/' . $post->slug)) }}&text={{ urlencode($post->title) }}" target="_blank" class="share-btn twitter">
                        <i class="bi bi-twitter"></i> Twitter
                    </a>
                    <a href="https://www.linkedin.com/shareArticle?mini=true&url={{ urlencode(url('/blog/' . $post->slug)) }}&title={{ urlencode($post->title) }}" target="_blank" class="share-btn linkedin">
                        <i class="bi bi-linkedin"></i> LinkedIn
                    </a>
                    <a href="https://wa.me/?text={{ urlencode($post->title . ' - ' . url('/blog/' . $post->slug)) }}" target="_blank" class="share-btn whatsapp">
                        <i class="bi bi-whatsapp"></i> WhatsApp
                    </a>
                </div>
            </div>
        </article>

        <!-- Related Posts -->
        @if($relatedPosts->count() > 0)
        <section class="related-posts" style="background: #f8fafc; padding: 4rem 2rem;">
            <div class="article-container">
                <h3>Related Articles</h3>
                <div class="related-grid">
                    @foreach($relatedPosts as $related)
                        <a href="{{ url('/blog/' . $related->slug) }}" class="related-card">
                            <img src="{{ $related->featured_image }}" alt="{{ $related->title }}">
                            <div class="related-card-content">
                                <h4>{{ $related->title }}</h4>
                                <p>{{ Str::limit($related->excerpt, 100) }}</p>
                                <div style="display: flex; align-items: center; gap: 0.5rem; margin-top: 1rem; color: #3b82f6; font-weight: 600; font-size: 0.875rem;">
                                    <span>Read More</span>
                                    <i class="bi bi-arrow-right"></i>
                                </div>
                            </div>
                        </a>
                    @endforeach
                </div>
            </div>
        </section>
        @endif

        <!-- Back to Blog -->
        <div class="back-to-blog">
            <a href="{{ url('/blog') }}">
                <i class="bi bi-arrow-left"></i>
                Back to All Articles
            </a>
        </div>
    </main>

    @include('partials.footer')
</body>
</html>
